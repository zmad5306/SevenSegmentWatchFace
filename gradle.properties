org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# Watch Face Format APKs must not contain code, so skip AGP's built-in Kotlin (and its stdlib).
android.builtInKotlin=false
